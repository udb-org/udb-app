import React from "react";
import { PlayIcon, SaveIcon, StopCircleIcon } from "lucide-react";

export const Icons={
    "play":<PlayIcon size={14}/>,
    "stop":<StopCircleIcon size={14}/>,
    "save":<SaveIcon size={14}/>,
}